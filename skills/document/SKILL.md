---
name: document
description: Write or improve documentation — READMEs, architecture overviews, module/API docs, deployment guides — grounded in the real implementation. Updates existing docs instead of duplicating them.
---

# ultrapower: document

Inspect the real code first (CodeGraph / Read) and reuse the project's existing
terminology and structure. Update existing docs rather than creating parallel
ones; do not accumulate redundant files. Be accurate over comprehensive —
document what the code actually does, not what it ideally might. Keep it lean.
